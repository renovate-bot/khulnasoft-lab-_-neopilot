# Authentication in CLI

You must authenticate with NEOPILOT CLI before using it. Run the following command to sign in into your NEOPILOT account:

<CodeGroup isTerminalCommand>
```bash
neopilot auth login
```
</CodeGroup>